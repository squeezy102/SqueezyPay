# SqueezyPay Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to the repository

# Encryption key for credentials and payment methods
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# WARNING: If this key is lost, all encrypted data is unrecoverable
SQUEEZYPAY_ENCRYPTION_KEY=

# Plaid API credentials (free developer account at https://dashboard.plaid.com)
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
